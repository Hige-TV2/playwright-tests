# Utilities

Pure helper functions that don't belong to a specific page.
(e.g., Date formatting, Auth token handling, API wrappers).

**Contents:**

- `test-helpers.js` - Reusable test functions for common assertions
- `debug/` - Debug utility specs for troubleshooting tests (see debug/README.md)
